{
  "name": "@chomp/watchtower",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Maintain-stage monitoring service. Deterministic control bands; Copilot SDK for diagnosis.",
  "license": "MIT",
  "engines": { "node": ">=22" },
  "scripts": {
    "watch": "node --experimental-strip-types watchtower.ts",
    "dry-run": "node --experimental-strip-types watchtower.ts --dry-run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "@github/copilot-sdk": "^1.0.11" },
  "devDependencies": { "@types/node": "^22.10.5", "typescript": "^5.7.3" }
}
